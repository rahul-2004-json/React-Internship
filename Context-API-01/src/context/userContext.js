import React from "react";

//Context is nothing but a global variable only
const UserContext = React.createContext();

export default UserContext;

// After making the context we will have to use a provider to get context into action.
